buat x = 10;
buat xy = x;
tulis(xy); 

buat hari = "Selasa";
buat ucapan = match hari { "Senin" => "Awali minggu", "Selasa" | "Rabu" => "Hello Semangat Kerja Baru" };

asinkron fungsi sayTo(){
    Desimal x = 20.2;
    kembalikan "Hello Handrawan";
}

hasil? value = await sayTo();

tulis(value, ucapan);